CLASSIFICATION = factor(CLASS5)
uniqueVector = unique(CLASSIFICATION)
nClasses = length(uniqueVector)
priorStat = rep(1/nClasses,nClasses)

#Make variable names accessible during session
#load  a csv text file, named lvinorm
xVarSel <- read.csv("D:\\Rwd\\Rdata\\Input\\XVARSEL.txt",header=T,row.names=1, stringsAsFactors=FALSE, 
	strip.white=TRUE, na.strings = c("NA",""))
#Make variable names accessible during session
attach(xVarSel)
#
nRows <- length(xVarSel[,1])
#varNameList <- lapply(xVarSel[,1], as.character)
xyDataset <- NULL
xVarNameList <- NULL
#Get the list of elgible xVariables and put them in a vector
for (i in 1:nRows)
{if (xVarSel[i,2]==1)
	xVarNameList <- c(xVarNameList,varNameList[[i]])
	xyDataset <- cbind(xyDataset,get(varNameList[[i]]))
} 
xyDataset <- cbind(xyDataset, CLASSIFICATION)
